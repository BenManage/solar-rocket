import { AppLayout } from "../layouts/AppLayout";

const Preferences = (): JSX.Element => {
  return (
    <AppLayout title="Preferences">
      <div>Preferences!</div>
    </AppLayout>
  );
};

export { Preferences };
